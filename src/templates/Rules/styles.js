import styled from 'styled-components';

export const Wrapper = styled.section`
	margin: 0 auto;
`;

export const RulesList = styled.article`
	max-width: 1000px;
	margin: 0 auto;
	padding: 1.5em 0;
`;
